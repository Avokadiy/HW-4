export function calculateAverageNumber(firstN, secondN, thirdN) {
    return (firstN + secondN + thirdN) / 3;
}

export function showAverageNumber(averageNumberRes) {
    alert("Среднее значение трёх цифр равен " + averageNumberRes);
}